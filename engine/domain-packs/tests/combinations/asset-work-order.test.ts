import assert from'node:assert/strict';import test from'node:test';import{activateProduction,composeProduction}from'../helpers/production-packs';
test('asset and work order production combination is deterministic and activatable',()=>{const ids=['asset_registry','work_order_service','asset_work_order_bridge'],a=composeProduction(ids),b=composeProduction([...ids].reverse());assert.equal(a.canGenerate,true,a.summary);assert.deepEqual(a.lock,b.lock);assert.deepEqual(a.lock!.dependencyOrder,['asset_registry','work_order_service','asset_work_order_bridge']);const entities=a.blueprint!.entities as any[],work=entities.find(x=>x.id==='work_order');assert.ok(work.fields.some((x:any)=>x.id==='asset_code'));assert.deepEqual(activateProduction(a).activationOrder,a.lock!.dependencyOrder);});
