import assert from'node:assert/strict';import test from'node:test';import{activateProduction,composeProduction}from'../helpers/production-packs';
test('reference production combination exposes twelve core modules and three bridges',()=>{const ids=['asset_registry','work_order_service','inspection_rectification','asset_work_order_bridge','asset_inspection_bridge','inspection_work_order_bridge'],r=composeProduction(ids);assert.equal(r.canGenerate,true,r.summary);assert.deepEqual(r.lock!.dependencyOrder,['asset_registry','inspection_rectification','asset_inspection_bridge','work_order_service','asset_work_order_bridge','inspection_work_order_bridge']);assert.equal((r.blueprint!.modules as any[]).length,12);assert.equal((r.blueprint!.plugins as any[]).length,6);const host=activateProduction(r);assert.deepEqual(host.activationOrder,r.lock!.dependencyOrder);assert.ok(host.lifecycleBlockers['asset.deactivation.work_order']);assert.ok(host.lifecycleBlockers['asset.deactivation.inspection']);assert.ok(host.completionHandlers['inspection.abnormal.work_order']);});
