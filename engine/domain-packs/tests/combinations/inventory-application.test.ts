import assert from'node:assert/strict';import test from'node:test';import{activateProduction,composeProduction}from'../helpers/production-packs';
test('inventory and application production combination locks approval deduction',()=>{const ids=['inventory_batch','application_archive','inventory_application_bridge'],r=composeProduction(ids);assert.equal(r.canGenerate,true,r.summary);assert.deepEqual(r.lock!.dependencyOrder,['application_archive','inventory_batch','inventory_application_bridge']);const link=(r.blueprint!.entities as any[]).find(x=>x.id==='application_inventory_issue');assert.equal(link.retention,'append_only');const host=activateProduction(r);assert.ok(host.domainCommands['inventory.application.command']);assert.ok(host.completionHandlers['application.approval.inventory']);});
