import test from'node:test';import assert from'node:assert/strict';import{AppError}from'../../../desktop-runtime/src/shared/errors';import{ApplicationArchiveService}from'../../packs/application_archive/runtime/application-service';import{applicationContext,archiveManager,createApplicationRuntime}from'../helpers/application-runtime';
test('creates and renews immutable certificate versions atomically',t=>{const x=createApplicationRuntime(t),s=new ApplicationArchiveService();let first=x.database.transaction(c=>s.createCertificate({certificateKey:'license',businessVersion:'1.0',name:'经营证照',certificateNo:'SYN-001',issuedAt:'2026-01-01',expiresAt:'2026-12-31',fileVersionCode:null},applicationContext(c,archiveManager)));assert.equal(first.status,'active');const second=x.database.transaction(c=>s.renewCertificate({previousCertificateId:first.certificateId,expectedPreviousVersion:1,businessVersion:'2.0',name:'经营证照',certificateNo:'SYN-002',issuedAt:'2026-12-01',expiresAt:'2027-12-31',fileVersionCode:null},applicationContext(c,archiveManager)));assert.equal(second.status,'active');assert.deepEqual((x.database.prepare('SELECT business_version,status,version FROM biz_certificate ORDER BY id').all()as unknown as Array<Record<string,unknown>>).map(row=>({...row})),[{business_version:'1.0',status:'superseded',version:2},{business_version:'2.0',status:'active',version:1}]);});
test('rejects impossible dates duplicate versions and stale renewal',t=>{const x=createApplicationRuntime(t),s=new ApplicationArchiveService();assert.throws(()=>x.database.transaction(c=>s.createCertificate({certificateKey:'bad',businessVersion:'1.0',name:'证照',certificateNo:'X',issuedAt:'2026-02-30',expiresAt:null,fileVersionCode:null},applicationContext(c,archiveManager))),code('VALIDATION_FAILED'));const first=x.database.transaction(c=>s.createCertificate({certificateKey:'license',businessVersion:'1.0',name:'证照',certificateNo:'X',issuedAt:'2026-01-01',expiresAt:null,fileVersionCode:null},applicationContext(c,archiveManager)));assert.throws(()=>x.database.transaction(c=>s.renewCertificate({previousCertificateId:first.certificateId,expectedPreviousVersion:99,businessVersion:'2.0',name:'证照',certificateNo:'Y',issuedAt:'2026-02-01',expiresAt:null,fileVersionCode:null},applicationContext(c,archiveManager))),code('VERSION_CONFLICT'));});
function code(expected:string){return(e:unknown)=>e instanceof AppError&&e.code===expected;}
