import test from'node:test';import assert from'node:assert/strict';import path from'node:path';import{loadPack}from'../../src/catalog/load-pack';import{PackRegistry}from'../../src/catalog/registry';import{composeDomainPacks}from'../../src/composition/compose';
test('allows a complete archive pack to extend every project container',()=>{const registry=new PackRegistry();registry.register(loadPack(path.resolve(__dirname,'..','..','packs','project_task')));registry.register(loadPack(path.resolve(__dirname,'..','fixtures','packs','project-archive-bridge')));const result=composeDomainPacks({blueprintSchemaVersion:'1.0',runtimeVersion:'1.0.0',software:{id:'project_archive',name:'项目归档软件',version:'1.0.0',purpose:'项目归档',targetUsers:['项目'],boundaries:['离线'],loginMode:'required'},selections:[{id:'project_task',version:'1.0.0',config:{}},{id:'application_archive',version:'1.0.0',config:{}}],coverage:{supported:['项目归档'],unsupported:[]},materials:{developmentPurpose:'桥接验收',industry:'企业',technicalFeatures:['扩展']}},registry);assert.equal(result.canGenerate,true,result.summary);const entities=(result.blueprint as any).entities;for(const id of['project','project_task','project_risk','deliverable']){const entity=entities.find((x:any)=>x.id===id);assert.ok(entity.fields.some((x:any)=>x.id==='archive_code'));assert.ok(entity.relations.some((x:any)=>x.id===`${id}_archive`));}for(const id of['project','project_task']){const entity=entities.find((x:any)=>x.id===id);assert.equal(entity.detailTabs.length,1);assert.equal(entity.createSources.length,1);}});
